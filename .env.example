BOT_TOKEN=
# Optional: Redis for durable domain data + sessions (production)
# REDIS_URL=redis://localhost:6379
# Optional: Telegram user id of the bot owner (food DB, system settings)
# OWNER_TELEGRAM_ID=
# Optional: OpenRouter key for vision-based photo calorie estimates
# OPENROUTER_API_KEY=
